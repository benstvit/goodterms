<h2>All you Items:</h2>

<ul>
  <% if @items.present? %>
    <h3>You have no items</h3>
  <% else %>
    <% @items.each do |item|  %>
        <li><%= link_to item.item_name, item_path(item) %></li>
      <% end  %>
  <% end %>
</ul>
