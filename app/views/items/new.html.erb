 <div class="item-container">
  <div class="container">
    <h2>Create an Item:</h2>

    <%= simple_form_for(@item) do |f| %>
      <%= f.input :item_name %>
      <%= f.input :description %>
      <%= f.input :photo, as: :file %>
      <%= f.submit %>
    <% end %>
  </div>
</div>
